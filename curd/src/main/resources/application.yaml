spring:
  datasource:
    url: jdbc:mysql://localhost:3307/curd_op
    username: root
    password: root
    driver: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

